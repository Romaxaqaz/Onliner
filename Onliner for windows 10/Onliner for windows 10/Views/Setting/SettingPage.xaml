<Page
    x:Class="Onliner_for_windows_10.Views.Setting.SettingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Onliner_for_windows_10.Views.Setting"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Template10.Controls"
    xmlns:mytoolkit="using:WinRTXamlToolkit.Controls"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource BackGroundCustomColorBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <controls:PageHeader Text="Настройки" FontWeight="Bold"
                             Frame="{x:Bind Frame}"
                             Background="{ThemeResource BackGroundCustomHeaderColorBrush}"
                             Foreground="{ThemeResource ForegroundCustomOtherBlackBrush}"
                             VisualStateNarrowMinWidth="1"
                             BackButtonVisibility="Collapsed"/>

        <Pivot x:Name="SettingPivot" SelectionChanged="SettingPivot_SelectionChanged" Grid.Row="1">
            <Pivot.Items>
                <PivotItem Header="Основные">
                    <StackPanel>
                        <CheckBox x:Name="AutoUpdateCheckBox" Content="Автоматически обновлять ленту новостей при старте" Margin="5"/>
                        <CheckBox x:Name="LoadImageCheckBox" Content="Загружать изображения" Margin="5"/>
                    </StackPanel>
                </PivotItem>
                <PivotItem Header="Оформление">
                    <StackPanel>
                        <ComboBox Header="Фон" SelectedIndex="0" Margin="5">
                            <ComboBoxItem Content="светлый"/>
                            <ComboBoxItem Content="темный"/>
                        </ComboBox>
                        <Button Content="Set dark theme" Width="100" Height="50" Margin="5" Click="Button_Click"/>
                    </StackPanel>
                </PivotItem>
                <PivotItem Header="Контент">
                    <StackPanel MaxWidth="600">
                        <ComboBox Header="Количество новостей, сохраняемых в кэш" SelectedIndex="0" Margin="5">
                            <ComboBoxItem Content="50"/>
                            <ComboBoxItem Content="100"/>
                            <ComboBoxItem Content="150"/>
                        </ComboBox>
                        <Grid MaxWidth="500" HorizontalAlignment="Stretch" Margin="5">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text=" Лента новостей сохраняется в кэш, для дальнейшей загрузки их без интернета" MaxWidth="600" HorizontalAlignment="Left" TextWrapping="Wrap" Grid.Row="0"/>
                            <Grid Grid.Row="1" Margin="5">
                                <mytoolkit:RingSlice StartAngle="0" EndAngle="359" Fill="LightGray" Radius="50" InnerRadius="40"/>
                                <mytoolkit:RingSlice Name="RingSlice" StartAngle="0" EndAngle="100" Fill="Gray" Radius="50" InnerRadius="40"/>
                                <TextBlock x:Name="Size" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                            <Button x:Name="ClearDB" Content="Очистить кэш" Grid.Row="2" Margin="5" HorizontalAlignment="Center" Click="ClearDB_Click"/>

                        </Grid>
                    </StackPanel>
                </PivotItem>
            </Pivot.Items>
        </Pivot>
    </Grid>
</Page>
